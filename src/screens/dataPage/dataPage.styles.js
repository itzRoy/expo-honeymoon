import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      flex: 1,
      justifyItems: 'center',
    },
    list: {
      alignContent: 'center',
      paddingBottom: 250,
    },
  });
export default styles